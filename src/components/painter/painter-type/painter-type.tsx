import React from 'react';
import classnames from 'classnames';

import styles from './painter-type.less';

export interface PainterTypeProps {

}

export const PainterType: React.FC<PainterTypeProps> = ({ }) => {
    
    return (
        <div className={classnames(styles.container)}>

        </div>
    );
};

export default PainterType;